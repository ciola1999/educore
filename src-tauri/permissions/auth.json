{
  "identifier": "default",
  "description": "Permit all local application commands",
  "commands": {
    "allow": [
      "login",
      "authenticate_with_biometric",
      "set_password",
      "verify_password",
      "get_sync_config",
      "set_sync_config"
    ]
  }
}
